# Wildfire Containment & Resource Deployment
# Core dependencies (no PyTorch — install separately with CPU wheel for CI speed)
# See: pip install torch --index-url https://download.pytorch.org/whl/cpu

numpy>=1.21.0
pyyaml>=6.0
matplotlib>=3.5.0
mlflow>=2.10.0
